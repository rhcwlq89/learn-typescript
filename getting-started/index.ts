function sum(a: number, b: number): number {
    return a + b;
}

sum(10, 20);

// 컴파일이 필요함. (ts -> js)
/**
 * 컴파일을 위해서 node 버전
 * npm i typescipt -g
 * 사용법 (tsc)
 * tsc index.ts
 * 
 * 웹 태스크 매니져 (webpack)을 쓰면 tsc 하는 것보다 편해짐.
 */

